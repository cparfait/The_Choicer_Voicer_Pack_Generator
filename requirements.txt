fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9
yt-dlp>=2025.1.1

# Transcription automatique (optionnel, installe ~1-2 Go de dependances).
# Decommente la ligne suivante, ou lance : pip install faster-whisper
# faster-whisper>=1.0.3
